# Copy to .env.local for development. Never commit .env.local.
#
# This is the ONLY value the frontend needs, and the only one that is safe to
# expose: anything prefixed with VITE_ is compiled into the public bundle.
# Never put a database URL, service key, JWT secret or API key here.

VITE_API_BASE_URL=http://localhost:4000
